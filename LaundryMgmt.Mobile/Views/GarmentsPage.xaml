<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:LaundryMgmt.Mobile.ViewModels"
             xmlns:models="clr-namespace:LaundryMgmt.Mobile.Models"
             xmlns:views="clr-namespace:LaundryMgmt.Mobile.Views"
             xmlns:behaviors="clr-namespace:LaundryMgmt.Mobile.Behaviors"
             x:Class="LaundryMgmt.Mobile.Views.GarmentsPage"
             Shell.TabBarIsVisible="False"
             x:DataType="vm:GarmentsViewModel"
             Title="Garments">

    <Grid RowDefinitions="Auto,Auto,Auto,Auto,*">
        <views:ModuleBreadcrumb Grid.Row="0" CurrentModule="garments" />

        <Picker Grid.Row="1" Margin="16,8" ItemsSource="{Binding Categories}" ItemDisplayBinding="{Binding Name}"
                SelectedItem="{Binding SelectedCategory}" Title="Filter by category" />

        <Button Grid.Row="2" Text="+ New Garment" HorizontalOptions="End" Margin="16,0,16,8"
                Command="{Binding NewGarmentCommand}" IsVisible="{Binding CanEditMasterData}" />

        <Label Grid.Row="3" Text="{Binding ErrorMessage}" TextColor="{StaticResource Danger}"
               IsVisible="{Binding ErrorMessage, Converter={StaticResource IsNotNullConverter}}"
               Padding="16,8" BackgroundColor="{StaticResource DangerLight}" />

        <RefreshView Grid.Row="4" IsRefreshing="{Binding IsRefreshing}" Command="{Binding RefreshCommand}">
            <CollectionView ItemsSource="{Binding Garments}"
                            RemainingItemsThreshold="3"
                            RemainingItemsThresholdReachedCommand="{Binding LoadMoreCommand}">
                <CollectionView.EmptyView>
                    <VerticalStackLayout Padding="24" Spacing="8" VerticalOptions="Center">
                        <Label Text="🧺" FontSize="40" HorizontalTextAlignment="Center" />
                        <Label Text="No garments found." HorizontalTextAlignment="Center" Style="{StaticResource SubtleLabel}" />
                    </VerticalStackLayout>
                </CollectionView.EmptyView>

                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:GarmentListItem">
                        <Frame Style="{StaticResource ListCard}" Padding="12,10" MinimumHeightRequest="72">
                            <Frame.GestureRecognizers>
                                <TapGestureRecognizer
                                    CommandParameter="{Binding .}"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenGarmentCommand}" />
                            </Frame.GestureRecognizers>
                            <Grid ColumnDefinitions="Auto,*">
                                <Border Grid.Column="0" StrokeShape="RoundRectangle 10" Stroke="Transparent"
                                        BackgroundColor="{StaticResource PrimaryLight}" WidthRequest="44" HeightRequest="44" Padding="0"
                                        VerticalOptions="Center" Margin="0,0,12,0">
                                    <Grid>
                                        <Label Text="{Binding Name, Converter={StaticResource GarmentIconConverter}}"
                                               FontSize="22" HorizontalOptions="Center" VerticalOptions="Center" />
                                        <Image behaviors:TrustedImage.Url="{Binding ImageUrl, Converter={StaticResource ResolvedImageUrlConverter}}"
                                               Aspect="AspectFill" IsVisible="{Binding ImageUrl, Converter={StaticResource IsNotNullConverter}}" />
                                    </Grid>
                                </Border>
                                <VerticalStackLayout Grid.Column="1" VerticalOptions="Center">
                                    <Label Text="{Binding Name}" FontAttributes="Bold" LineBreakMode="WordWrap" />
                                    <Label Text="{Binding CategoryName}" Style="{StaticResource SubtleLabel}" LineBreakMode="WordWrap" />
                                </VerticalStackLayout>
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>
    </Grid>

</ContentPage>
