<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.laundarywala.app" android:versionCode="1">
	<!-- Remove explicit icon references to avoid build-time missing resource errors.
         Restore android:icon/android:roundIcon if you add appropriate mipmap resources
         or re-enable MauiIcon in the project file. -->
	<application android:allowBackup="true" android:supportsRtl="true" android:label="Laundry Wala"></application>
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.CAMERA" />
	<uses-feature android:name="android.hardware.camera" android:required="false" />
</manifest>